package interpreter.bytecode.operator;

/**
 * This abstract class doesn't really bring anything to the table.
 * It just exists so that Math & Logical Operators come from the
 * same parent class and we can use one HashMap instead of two
 */

public abstract class OpsObject {}
